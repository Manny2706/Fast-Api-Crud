version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: primetrade
      POSTGRES_PASSWORD: primetradepass
      POSTGRES_DB: primetrade
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  pgdata:
